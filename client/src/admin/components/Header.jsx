const Header = () => {
  return (
    <header
      style={{
        height: "64px",
        padding: "16px 24px",
        borderBottom: "1px solid #ddd",
        display: "flex",
        alignItems: "center",
        background: "#fff",
      }}
    >
      <h2 style={{ margin: 0 }}>Admin Dashboard</h2>
    </header>
  );
};

export default Header;
